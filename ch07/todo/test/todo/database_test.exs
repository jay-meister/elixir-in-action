defmodule DatabaseTest do
  use ExUnit.Case

  # setup do
  #   Database.Server.purge_db()
  # end

  # test "Database.put()" do
  #   assert :ok = Database.put("test-file", %{a: :map})
  # end

  # test "Database.get()" do
  #   :ok = Database.put("test-file", %{a: :map})
  #   assert %{a: :map} = Database.get("test-file")
  # end
end
